import pool from '../../utils/db.js';

const getSubmissions = async (req, res) => {
    try {

    }

    catch (error) {
        console.error(error);
    }

    finally {
        
    }
};

export default getSubmissions;