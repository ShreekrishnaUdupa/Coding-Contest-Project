import pool from '../../utils/db.js';

const runCode = async (req, res) => {
    try {

    }

    catch (error) {
        console.error(error);
    }

    finally {
        
    }
};

export default runCode;