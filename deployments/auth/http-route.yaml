apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: auth-http-route
  namespace: default

spec:
  parentRefs:
    - name: envoy-gateway
      namespace: default

  rules:
    -backendRe